name: Perf Report

on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Git ref to run against (branch, tag, or SHA)'
        required: false
        default: ''
      node-version:
        description: Node.js version
        required: false
        default: '20'
      package-manager:
        description: Package manager to use (pnpm or npm)
        required: false
        default: pnpm

permissions:
  contents: write

concurrency:
  group: perf-report-${{ github.ref }}
  cancel-in-progress: false

jobs:
  generate:
    name: Generate perf-latest.md
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.ref || github.ref }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ github.event.inputs['node-version'] || '20' }}

      - name: Setup pnpm
        if: ${{ github.event.inputs['package-manager'] != 'npm' }}
        uses: pnpm/action-setup@v4
        with:
          version: 9
          run_install: false

      - name: Cache pnpm store
        if: ${{ github.event.inputs['package-manager'] != 'npm' }}
        uses: actions/cache@v4
        with:
          path: ~/.pnpm-store
          key: ${{ runner.os }}-pnpm-${{ hashFiles('**/pnpm-lock.yaml') }}-${{ github.ref }}
          restore-keys: |
            ${{ runner.os }}-pnpm-

      - name: Install dependencies
        run: |
          if [ "${{ github.event.inputs['package-manager'] }}" = "npm" ]; then
            npm ci
          else
            pnpm install --frozen-lockfile
          fi

      - name: Generate performance report
        run: |
          if [ "${{ github.event.inputs['package-manager'] }}" = "npm" ]; then
            npm run perf:generate
          else
            pnpm run perf:generate
          fi

      - name: Upload report artifact
        uses: actions/upload-artifact@v4
        with:
          name: perf-latest
          path: docs/perf-latest.md

      - name: Commit and push changes (if any)
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          if git diff --quiet docs/perf-latest.md; then
            echo "No changes in perf-latest.md"
            exit 0
          fi
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/perf-latest.md
          git commit -m "chore(perf): update perf-latest.md [skip ci]"
          git push
